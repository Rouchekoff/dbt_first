[project]
name = "dbt-init"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dbt-core",
    "dbt-mysql",
    "pandas",
    "pymysql",
    "sqlalchemy",
    "sqlalchemy_utils",
    "cryptography"
]

